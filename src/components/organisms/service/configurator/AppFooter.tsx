import React from "react";

const AppFooter = () => {
  return (
    <footer className="mt-8 text-sm text-slate-500">
      Built for senior dev workflows — tweak this file to integrate with your
      backend (save-to-db / generate ticket / create estimate PDF).
    </footer>
  );
};

export default AppFooter;
