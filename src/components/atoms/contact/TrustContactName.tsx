import Description from "@/shared/ui/Description";
import React from "react";

const TrustContactName = ({ name }: { name: string }) => {
  return <Description>{name}</Description>;
};

export default TrustContactName;
