import React from "react";

const ContactFormTitle = () => {
  return (
    <h2 id="contact-form" className="text-2xl text-center pb-2">
      Send as a message
    </h2>
  );
};

export default ContactFormTitle;
